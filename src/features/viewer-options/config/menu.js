/** Approximate rendered size, used to keep the menu inside the viewport. */
export const MENU_WIDTH_PX = 200;
export const MENU_HEIGHT_PX = 170;

export const SUBMENU_WIDTH_PX = 130;

export const SUBMENU_OVERHANG_PX = 60;
