/** Radians of model rotation per pixel dragged. */
export const ROTATION_SPEED = 0.005;

/** Pointer travel below this many pixels counts as a click, not a drag. */
export const CLICK_DRAG_THRESHOLD_PX = 5;
