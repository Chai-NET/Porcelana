export const CAMERA = {
  fov: 75,
  near: 0.1,
  far: 1000,
  initialDistance: 5,
  minDistance: 2.5,

  unlockedMinDistance: 0.05,
  maxDistance: 10,
};

export const NORMALIZED_MODEL_SIZE = 3.5;

export const PAN_SPEED_PER_UNIT = 0.001;

export const ZOOM_SPEED_PER_PIXEL = 0.005;

export const IDLE_ROTATION_SPEED = 0.003;

export const TURNTABLE_ROTATION_SPEED = 0.006;

export const TURNTABLE_SPEEDS = [0.3, 0.6, 1, 2];

export const DEFAULT_TURNTABLE_SPEED = 0.6;
